---
title: Last lines from a file
taxonomy:
  snippet-tags:
    - file I/O
    - iterables
    - collections.deque
python_version: '3'
---

Using the parameter `maxlen` of the data structure `collections.deque` allows you to fetch the last `n` elements from any iterable you pass it, which also works to get the last `n` lines from a file.
