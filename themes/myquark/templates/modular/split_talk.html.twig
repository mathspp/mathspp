{% set grid_size = theme_var('grid-size') %}
{% set image = page.media.images|first %}
{% set splits = content | split("===") %}
{% set align = page.header.image_align|default('right') %}

<section class="section modular-text {{ page.header.class}}">
    <section class="container {{ grid_size }}">
        <div class="columns {{ align }}">
            {% if page.header.title %}
                {{ add_anchors("<h2>" ~ page.header.title ~ "</h2>") }}
            {% endif %}
            <div class="column col-6 col-md-12">
                {% if align == 'right' %}
                    {% if page.header.event %}
                        {{ add_anchors("<h3>" ~ page.header.event ~ "</h3>")}}
                    {% endif %}
                    {{ splits[0] }}
                {% else %}
                    {# This is the video or the image to embed. #}
                    {{ splits[1] }}
                {% endif %}
            </div>
            <div class="column col-6 col-md-12">
                {% if align == 'left' %}
                    {% if page.header.event %}
                        {{ add_anchors("<h3>" ~ page.header.event ~ "</h3>")}}
                    {% endif %}
                    {{ splits[0] }}
                {% else %}
                    {# This is the video or the image to embed. #}
                    {{ splits[1] }}
                {% endif %}
            </div>
        </div>
    <hr>
    </section>
</section>
