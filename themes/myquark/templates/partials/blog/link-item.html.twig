{#
Custom template written for the link blog section of my website.
Expects link items to have the following frontmatter variables:
- link
- title
- via (optional)
- date

It can also include arbitrary Markdown content.
#}

<div class="link-item">
    <div class="link-header">
        <p><a class="link-title" href="{{ link-item.header.link }}" target="_blank">{{ link-item.title }}</a>
        {% if link-item.header.via %}
            <a class="link-via" href="{{ link-item.header.via }}" target="_blank">(via)</a>
        {% endif %}
        {% if link-item.header.author %}
            by {{ link-item.header.author }}
        {% endif %}
        on
        <span class="link-date">
            {{ link-item.date|date("d-m-Y H:i") }}
        </span></p>
    </div>
    <div class="link-content">
        {{ link-item.content|raw }}
    </div>
    <hr />
</div>
